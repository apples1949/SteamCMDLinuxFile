name: SteamCMD Upload

on:
  workflow_dispatch:  # 仅保留手动触发

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Setup Environment
      run: |
        sudo apt-get update
        sudo apt-get install -y lib32gcc-s1 python3-pip
        sudo timedatectl set-timezone Asia/Shanghai

    - name: Install Python Dependencies
      run: |
        pip3 install requests base58 pycryptodomex

    - name: Prepare Directories
      run: |
        mkdir -p "$GITHUB_WORKSPACE/build"  
        WORKDIR="$HOME/steamcmd_build"
        mkdir -p "$WORKDIR"
        cd "$WORKDIR"
        echo "Current dir: $(pwd)"

    - name: Download SteamCMD
      run: |
        cd "$HOME/steamcmd_build"
        wget -q https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz
        tar -xzf steamcmd_linux.tar.gz
        rm steamcmd_linux.tar.gz
        ./steamcmd.sh +quit
        chmod -R a+r *

    - name: Create SteamCMD Package
      run: |
        tar -czvf "$GITHUB_WORKSPACE/build/steamcmd_linux.tar.gz" \
          --exclude='linux32/steamclient.so' \
          -C "$HOME/steamcmd_build" \
          linux32 \
          package \
          steamcmd.sh
    
        ls -lh "$GITHUB_WORKSPACE/build"
        echo "Package path: $GITHUB_WORKSPACE/build/steamcmd_linux.tar.gz"

    - name: Compress Package Contents
      run: |
        cd "$HOME/steamcmd_build"
        (cd package && tar -czvf ../package.tar.gz ./*)
        mv package.tar.gz "$GITHUB_WORKSPACE/build/"
        ls -lh "$GITHUB_WORKSPACE/build"
        echo "Package path: $GITHUB_WORKSPACE/build/package.tar.gz"
        
    - name: Upload Package via Script
      run: |
        cd "$GITHUB_WORKSPACE"
        wget -q https://raw.githubusercontent.com/Aruelius/wenshushu/refs/heads/master/wss.py
        # 运行上传命令并将输出保存到文件
        python3 wss.py upload build/steamcmd_linux.tar.gz > upload_output.log 2>&1
        
        # 提取关键信息：空间信息和链接
        SPACE_INFO=$(grep "当前已用空间" upload_output.log || echo "")
        PRIVATE_LINK=$(grep "个人管理链接" upload_output.log | cut -d'：' -f2 || echo "")
        PUBLIC_LINK=$(grep "公共链接" upload_output.log | cut -d'：' -f2 || echo "")
        
        # 构造格式化的输出
        FORMATTED_OUTPUT=""
        if [ -n "$SPACE_INFO" ]; then
          FORMATTED_OUTPUT+="${SPACE_INFO}\n"
        fi
        if [ -n "$PRIVATE_LINK" ]; then
          FORMATTED_OUTPUT+="个人管理链接：${PRIVATE_LINK}\n"
        fi
        if [ -n "$PUBLIC_LINK" ]; then
          FORMATTED_OUTPUT+="公共链接：${PUBLIC_LINK}\n"
        fi
        
        # 保存到环境变量
        echo "UPLOAD_OUTPUT<<EOF" >> $GITHUB_ENV
        echo -e "$FORMATTED_OUTPUT" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV
        
        # 显示上传结果
        echo -e "\n📤 上传结果：\n${FORMATTED_OUTPUT}"
        
    - name: Update README with Upload Result
      run: |
        # 在README中定位或创建上传结果区域
        if grep -q "## 最新上传结果" README.md; then
          # 替换现有结果
          sed -i '/## 最新上传结果/,/## / { /## 最新上传结果/ { n; r /dev/stdin
          }; d }' README.md <<< "$UPLOAD_OUTPUT"
        else
          # 创建新结果区域
          echo -e "\n## 最新上传结果\n" >> README.md
          echo "$UPLOAD_OUTPUT" >> README.md
          echo -e "\n" >> README.md
        fi
        
        echo "✅ README已更新"
        cat README.md | tail -n 10
        
    - name: Commit & Push
      run: |
        cd "$GITHUB_WORKSPACE"
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@users.noreply.github.com"

        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

        if [ -n "$(git status --porcelain build/ README.md)" ]; then
          git add build/ README.md
          git commit -m "🤖 Auto Update SteamCMD ($(date +'%Y-%m-%d %H:%M'))"
          git pull --rebase origin test
          git push origin HEAD:test
          echo "🚀 更改已推送"
        else
          echo "🟢 无更改可提交"
        fi
